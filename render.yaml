services:
  - type: web
    name: dictionary-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements-api.txt
    startCommand: gunicorn api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - key: FRONTEND_ORIGIN
        sync: false
